pub struct Solution;

impl Solution {
    pub fn ways_to_split(mut nums: Vec<i32>) -> i32 {
        // let n = nums.len();
        // let (mut ptr_left, mut ptr_right) = (0, 0);
        // let mut ans = 0;

        // for i in 1..n {
        //     nums[i] += nums[i - 1];
        // }
        // // 

        1

        // ans as i32 % 1_000_000_007
    }
}

#[cfg(test)]
mod test {
    use super::*;
    #[test]
    fn test_p1712() {
        // 在此编写测试代码
        // assert_eq!(Solution::ways_to_split(vec![1, 1, 1]), 1);
    }
}
